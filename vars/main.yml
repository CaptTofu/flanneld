---

flannel_env_vars:
  FLANNEL_INTERFACE: "{{ flannel_interface }}"
  FLANNEL_NETWORK: "{{ flannel_network }}"
  FLANNEL_MTU: 1472
  FLANNELD_OPTS: "--etcd-endpoints=http://{{ etcd_host }}:{{ etcd_client_port }}"
